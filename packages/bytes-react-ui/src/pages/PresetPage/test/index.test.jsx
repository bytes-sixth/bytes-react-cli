import React from 'react'
import { render, screen } from '@testing-library/react'
import PresetPage from '../index'

it('PresetPage Renders Fine', () => {
  // eslint-disable-next-line react/jsx-filename-extension
  // TODO:  this page is too complext to test
  // render(<PresetPage />)
  // expect(screen.getByText('选择一套预设')).toBeInTheDocument()
})
